@model List<JediApp.Database.Domain.Currency>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>Name</th>
            <th>ShortName</th>
            <th>BuyAt</th>
            <th>SellAt</th>
            <th>Details</th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>@item.Name</td>
            <td>@item.ShortName</td>
            <td>@item.BuyAt</td>
            <td>@item.SellAt</td>
            <td>
                <a asp-controller="ExchangeOfficeBoard" asp-action="Edit" asp-route-id="@item.Id" asp-route-someName="some value">Edit</a>
                <a asp-controller="ExchangeOfficeBoard" asp-action="Details" asp-route-id="@item.Id" asp-route-someName="some value">Details</a>
                <a asp-controller="ExchangeOfficeBoard" asp-action="Delete" asp-route-id="@item.Id" asp-route-someName="some value">Delete</a>
            </td>
@*            <td>
                @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
            </td>*@
        </tr>
}
    </tbody>
</table>
